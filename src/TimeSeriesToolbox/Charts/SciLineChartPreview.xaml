<UserControl x:Class="TimeSeriesToolbox.Charts.SciLineChartPreview"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:TimeSeriesToolbox.Charts"
             xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
             xmlns:zoomingAndPanning="clr-namespace:TimeSeriesToolbox.Charts"
             mc:Ignorable="d" 
             d:DesignHeight="240" d:DesignWidth="320"
             BorderThickness="1" BorderBrush="DarkGray"
             d:DataContext="{d:DesignInstance local:SciLineChartPreview}">
    <UserControl.Resources>
        <zoomingAndPanning:ZoomingModeCoverter x:Key="ZoomingModeCoverter"></zoomingAndPanning:ZoomingModeCoverter>
    </UserControl.Resources>
    <Grid>
        <lvc:CartesianChart Series="{Binding SeriesCollection}" Zoom="{Binding ZoomingMode}">
            <lvc:CartesianChart.AxisY>
                <lvc:Axis Title="{Binding AxisY}" LabelFormatter="{Binding YFormatter}"></lvc:Axis>
            </lvc:CartesianChart.AxisY>
            <lvc:CartesianChart.AxisX>
                <lvc:Axis Title="{Binding AxisX}" Labels="{Binding Labels}"></lvc:Axis>
            </lvc:CartesianChart.AxisX>
        </lvc:CartesianChart>
    </Grid>
</UserControl>